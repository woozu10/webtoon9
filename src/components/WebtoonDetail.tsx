import type { Webtoon } from '../data/mockWebtoons'
type Props={item:Webtoon;onBack:()=>void}
export default function WebtoonDetail({item,onBack}:Props){return <main className="detail-page"><button className="back-button" onClick={onBack}>← 목록으로</button><header className="detail-hero"><div className="detail-cover">첫 번째<br/>기회</div><div><span className="eyebrow">{item.weekday}요일 연재 · {item.category}</span><h1>{item.title}</h1><p className="detail-subtitle">{item.subtitle}</p><p>{item.description??'상세 이야기는 준비 중입니다.'}</p>{item.creator?<small>글·그림 {item.creator}</small>:null}</div></header>{item.characters?.length?<section className="character-strip"><h2>등장인물</h2><div>{item.characters.map(n=><span key={n}>{n}</span>)}</div></section>:null}<section className="episode"><div className="episode-head"><span>무료 공개</span><h2>{item.episodeTitle??'첫 번째 이야기'}</h2></div>{item.scenes?.length?item.scenes.map((scene,i)=><article className={`scene scene-${i%3+1}`} key={scene.caption}><div className="scene-art"><strong>{i+1}</strong></div><p className="scene-caption">{scene.caption}</p>{scene.dialogue?<blockquote>{scene.dialogue}</blockquote>:null}</article>):<p>이 작품의 본문은 준비 중입니다.</p>}<div className="episode-end"><strong>1화 끝</strong><span>다음 이야기는 월요일에 계속됩니다.</span></div></section></main>}