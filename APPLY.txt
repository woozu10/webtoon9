WEBTOON9 v0.12 사이즈 조정 패치

포함 파일:
- src/styles.css

변경:
- 한 컷을 화면 정중앙에 배치
- 데스크톱 기준 너무 작거나 너무 꽉 차지 않게 width:min(650px, 72vw)로 조정
- 밝은 회색 배경 위에 이미지가 보이도록 조정
- 컷 아래 별도 자막/대사 박스는 계속 숨김
- 패널 번호는 숨김

적용:
GitHub repository에서 src/styles.css 파일만 같은 경로에 덮어쓰면 됩니다.
